---
- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update repositories
      apt:
        update_cache: yes
      changed_when: False
      when: ansible_distribution in ["Debian", "Ubuntu", "Pop!_OS"]

  roles:
    - base
    - role: workstation 
      when: ansible_default_ipv4.address in ["192.168.178.2",]